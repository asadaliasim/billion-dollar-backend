PORT=8000
MONGO_DB_URI=mongodb+srv://your-connection-string/billions
JWT_EXPIRATION=24h
JWT_SECRET=your-jwt-secret
JWT_REFRESH_TOKEN_SECRET=your-refresh-secret

# Stripe
STRIPE_SECRET_KEY=sk_test_your_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret
STRIPE_PRICE_PER_BLOCK_CENTS=1000

# App
# Comma-separated list of allowed frontend origins (used for Stripe redirect URLs)
FRONTEND_URL=https://fanwall2026.com,http://localhost:5173
PUBLIC_UPLOAD_PATH=./public/blocks

# Referrals
REFERRAL_ADMIN_SECRET=change-me-to-a-long-random-id
REFERRAL_COMMISSION_RATE=0.2
PIXELS_PER_BLOCK=100
